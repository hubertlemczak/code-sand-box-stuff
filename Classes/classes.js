class Animal {
  constructor(type, color) {
    this.type = type;
    this.color = color;
  }
}
const animalOne = new Animal('fox', 'blue');
const animalTwo = new Animal('cat', 'red');
console.log(animalOne, animalTwo);

const occupancy = () => {
  const enter = (numAdult, numChild) => {};
  const leave = () => {};
};
